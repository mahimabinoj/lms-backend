name: Backend CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend-flask

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Required for better SonarQube analysis

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run tests
        run: pytest || true

      # TASK 4: SNYK Integration (Dependency Vulnerability Scan)
      - name: Run Snyk to check for vulnerabilities
        uses: snyk/actions/python@master
        env:
          [cite_start]SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }} # Ensure this secret is added to GitHub 

      # TASK 3: SonarQube (SonarCloud) Integration
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.organization=${{ secrets.SONAR_ORGANIZATION }}
            -Dsonar.projectKey=${{ secrets.SONAR_PROJECT_KEY }}
            -Dsonar.sources=.
            -Dsonar.python.version=3
